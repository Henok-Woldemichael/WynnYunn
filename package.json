{
  "dependencies": {
    "build": "^0.1.4",
    "preview": "^0.1.3",
    "run": "^1.4.0",
    "three": "^0.158.0"
  },
  "devDependencies": {
    "vite": "^4.5.0"
  },
  "scripts": {
    "start": "devDependencies",
    "build": "echo 'build script executed'",
    "preview": "preview"
  }
}
